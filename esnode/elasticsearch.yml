node.master: false
node.data: true
network.host: 0.0.0.0
indices.recovery.max_bytes_per_sec: 100mb

discovery.zen.ping.unicast.hosts: ["esmaster", "127.0.0.1", "[::1]"]

thread_pool:
  bulk:
    queue_size: 50
